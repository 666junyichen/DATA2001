# 项目说明

## 基本信息

| 字段 | 内容 |
| --- | --- |
| 项目名称 | Spatial Resource Accessibility Analysis |
| 时间 | 公开版本不披露具体个人时间线；可在私人简历中补充月份和年份 |
| 项目阶段 | 数据建模、空间分析、评分模型、可视化展示 |
| 我的角色 | 数据分析与空间数据库建模贡献者 |
| 使用工具 | Python, pandas, GeoPandas, Shapely, SQLAlchemy, PostgreSQL, PostGIS, Folium, Matplotlib, Seaborn, Jupyter Notebook |
| 是否可以公开 | 可以公开项目方法、流程、结果摘要和非敏感成果；不公开个人信息、机构信息、数据库凭据和原始私有数据 |

## 我负责的内容

- 参与多源开放数据的清洗、类型转换和字段标准化。
- 将 CSV、TXT、JSON 和 shapefile 数据转换为适合空间分析的表结构。
- 使用 PostGIS `POINT` 和 `MULTIPOLYGON` 几何字段建模空间数据。
- 设计并实现基于 SA2 区域的资源评分指标。
- 使用 `ST_Contains` 和 `ST_Overlaps` 完成设施点、学区范围、犯罪热点与 SA2 区域的空间连接。
- 建立 SQL 视图链路，将原始指标转换为 z-score，再通过 sigmoid 函数组合为综合资源评分。
- 扩展基础模型，加入公共厕所、家庭暴力热点区域和非雇佣型建筑业企业等额外指标。
- 使用 Folium 生成交互式地图，并用 Seaborn/Matplotlib 检查资源评分与收入中位数之间的关系。

## 数据来源

公开文档中只保留数据类型与用途，不绑定个人或机构身份：

- 收入数据：用于收入中位数相关性检查。
- 人口数据：用于过滤低人口区域，并计算每千人资源指标。
- 商业数据：用于衡量零售和医疗健康资源。
- 公共交通站点数据：用于衡量交通可达性。
- 投票地点数据：用于衡量公共服务点位覆盖。
- 学校 catchment 数据：用于衡量教育资源覆盖。
- SA2 边界数据：用于区域空间聚合。
- 公共厕所数据：作为扩展公共设施指标。
- 犯罪热点数据：作为负向风险指标。
- 企业雇佣规模数据：作为扩展经济结构指标。

## 分析方法

- 数据清洗：处理缺失值、删除无地理坐标记录、统一数值类型、筛选 Greater Sydney 区域。
- 空间建模：将经纬度转换为 `POINT`，将 polygon 转换为 `MULTIPOLYGON`，统一 SRID 为 4326。
- 数据入库：创建 PostgreSQL/PostGIS 表，写入清洗后的表格和空间数据。
- 查询优化：为 SA2 code 和几何字段建立属性索引与 GIST 空间索引。
- 指标计算：按人口或面积标准化资源数量，避免大区域或人口差异直接主导评分。
- 标准化：对各项资源指标计算 z-score，使不同量纲可组合。
- 综合评分：用 sigmoid 函数将多个指标合成为 0 到 1 之间的资源充足度评分。
- 结果验证：用收入中位数做相关性检查，并通过地图观察空间分布。

## 最终结果

- 整合 10 类数据源，覆盖人口、收入、商业、交通、公共设施、教育边界和风险热点。
- 建立 10 张数据库表和 27 个 SQL 视图，形成从清洗到评分的可追踪数据管线。
- 设计 8 个资源相关指标：5 个基础指标和 3 个扩展指标。
- 最终扩展模型与收入中位数的相关系数为 `0.504201`，说明资源充足度评分与区域收入水平存在中等正相关。
- 生成静态地图和交互式 Folium HTML 地图，用于展示不同 SA2 区域的评分分布。

## 可以写进简历的量化成果

- Built a PostGIS-based spatial analytics pipeline integrating 10 open datasets across demographic, business, transport, public facility, education, and risk indicators.
- Designed 8 standardized resource accessibility indicators and combined them into a sigmoid-based composite score at SA2-region level.
- Created 10 database tables, 27 SQL views, and spatial indexes to support repeatable geospatial joins and scoring queries.
- Produced an interactive Folium choropleth map and validated model behavior through a median-income correlation check (`r = 0.504201`).

## 公开边界

- 可以公开：方法、工具、数据类型、指标公式、模型流程、结果摘要、地图导出文件。
- 不建议公开：个人身份信息、机构信息、数据库账号、数据库密码、原始受限数据。
- 建议检查：notebook 中如保留数据库连接配置文件名，应确保实际凭据文件没有上传。
