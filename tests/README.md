# Tests Directory

This directory contains unit tests for the project code.

## Running Tests

```bash
# Install pytest if needed
pip install pytest

# Run all tests
pytest

# Run with verbose output
pytest -v
```

## Writing Tests

Create test files with prefix `test_`:
- `test_data_processing.py`
- `test_analysis.py`
- `test_visualization.py`
