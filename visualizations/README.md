# Visualizations

This folder is reserved for visualization documentation if map outputs are reorganized later.

Current visualization artifact:

- `interactive_resource_accessibility_map.html`: exported Folium interactive choropleth map for the final well-resourced score.

The map supports project presentation by showing:

- SA2 region boundaries
- Final resource accessibility score
- Interactive popups with area-level attributes
- Layer controls and a minimap

Before publishing, verify that map popups contain only public area names, area codes, scores, and non-sensitive project attributes.
