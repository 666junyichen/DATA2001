# Notebooks

This folder is reserved for notebook documentation if the notebook files are reorganized later.

Current notebook artifacts are kept in the repository root:

- `FINAL.ipynb`: final workflow covering cleaning, database loading, scoring, extensions, correlation checks, and interactive mapping.
- `V14_pandas.ipynb`: pandas/GeoPandas scoring draft.
- `V15_toSQL.ipynb`: PostgreSQL/PostGIS scoring draft.

Before publishing, review notebook outputs for credentials, private paths, personal identifiers, or institution-specific references.
